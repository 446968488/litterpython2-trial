# 素材整理模板（把你的真实内容换进来）

本工具现阶段（阶段 0）的内容都写在 `data/course.js` 这一个文件里。
把你自己的视频 / 文本 / 作业按下面格式填进去，就能替换掉示例课程。

> 后期（阶段 5）我们会支持：直接把 mp4 / doc / pdf / ppt 拖进文件夹自动识别，不用手改这个文件。现在先照模板手填。

---

## 一、整体结构

打开 `data/course.js`，你会看到这样的结构：

```js
window.COURSE_DATA = {
  title: "课程标题",
  chapters: [
    {
      id: "ch1",            // 章节唯一编号，别重复
      title: "第1章 认识编程",
      lessons: [
        {
          id: "l1",          // 课时唯一编号，别重复
          title: "1.1 什么是编程",
          video: "videos/sample.mp4",   // 视频路径，没有就删掉这行
          markdown: `讲义内容（见下）`,
          exercises: [ /* 题目，见下 */ ]
        }
      ]
    }
  ]
};
```

---

## 二、视频怎么放

1. 把你的 mp4 文件放进 `videos/` 文件夹，例如 `videos/lesson1.mp4`
2. 在对应课时的 `video:` 字段填上路径：`video: "videos/lesson1.mp4"`
3. 没有视频的课时，直接**删除**这一行即可（页面会显示“本节为图文讲解”）

⚠️ 视频文件较大，不要塞进 `data/course.js`，一定是单独文件放 `videos/`。

---

## 三、文本讲义怎么写（Markdown）

讲义写在 `markdown:` 后面的反引号 `` ` `` 里（叫“模板字符串”）。支持这些写法：

```
# 一级标题
## 二级标题
**加粗**   *斜体*
`行内代码`
> 引用一段话
- 无序列表项
1. 有序列表项
\`\`\`python
代码块（开头三个反引号 + 语言名，结尾三个反引号）
\`\`\`
```

注意：如果讲义里**本身要出现三个反引号**，需要在每个反引号前加一个反斜杠 `\`。
（一般讲义用不到，遇到了再说。）

doc / pdf / ppt 怎么办？
- 阶段 0：把里面的文字**复制**到 markdown 字段里（图片可先省略或留空）。
- 阶段 5：我们加自动解析，你直接把原文件丢进 `materials/docs/` 即可。

---

## 四、作业（练习）怎么写

`exercises` 是一个数组，每道题是一个对象。支持三种类型：

### 1) 选择题 choice
```js
{
  type: "choice",
  question: "题目文字？",
  options: ["选项A", "选项B", "选项C", "选项D"],
  answer: 0,            // 正确选项是第几个，从 0 开始数（0=A,1=B...）
  explain: "答错时的解析（可选）"
}
```

### 2) 填空题 fill
```js
{
  type: "fill",
  question: "___ 是什么？",
  answer: "循环",        // 标准答案；多个答案用 | 分隔，如 "循环|loop"
  explain: "解析（可选）"
}
```

### 3) 开放题 open（不判分，只给参考）
```js
{
  type: "open",
  question: "谈谈你的理解？",
  answer: "参考答案文字……"
}
```

---

## 五、快速检查清单

- [ ] 每个 `id` 在文件里唯一（ch1/l1 这种）
- [ ] 视频文件确实在 `videos/` 里，路径拼写一致
- [ ] 选择题 `answer` 是数字（0 起），不是文字
- [ ] 改完保存后，双击 `index.html` 刷新看效果
