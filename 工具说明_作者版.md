# 《和小光一起学 Python》· 工具说明（作者版）

> 面向**作者 / 开发者**。记录目录结构、激活机制、更新与备份、视频/语音生成流水线、换课与改图标、依赖与发版清单。
> 使用者请看同包的《工具说明_用户版》。

---

## 1. 目录结构

```
电脑使用技巧/
├── 网课工具/                    ← 发给用户的完整工具（本目录即产品）
│   ├── index.html              # 双击打开；各资源带 ?v= 防缓存常量
│   ├── update.bat              # 一键更新器（Windows 双击更新）
│   ├── update.command          # Mac 启动器（可选）
│   ├── 双击打开.bat / 双击打开.command
│   ├── 更新说明.txt            # 给用户看的中文更新指引
│   ├── 工具说明_用户版.md       # 面向使用者
│   ├── 工具说明_作者版.md       # 本文件
│   ├── css/style.css           # 样式（图标/弹窗/激活门禁等）
│   ├── js/
│   │   ├── app.js              # ⭐ 主逻辑（课程/判分/语音/代码沙盒/激活/备份）
│   │   ├── vocab.js            # 单词渲染
│   │   ├── markdown.js         # 讲义 Markdown 渲染
│   │   ├── cover_icons.js      # 课程卡封面 SVG 图标表（COVER_ICON）
│   │   └── (libs) jsrsasign.min.js  # RSA 验签（激活用）
│   ├── data/
│   │   ├── course.js           # ⭐ 课程树/讲义/练习/单词（核心内容）
│   │   ├── code_practice.js    # 初级/中级代码练习题库（各 20 题，填空）
│   │   └── (可选) audio.js      # 内联 AUDIO_MAP / AUDIO_LEARN（语音映射）
│   ├── video/                  # 课程视频 mp4（c0l1…c13l4、py1、py2 等）
│   ├── audio/                  # 预生成语音 mp3（按章节目录）
│   │   ├── cXlY/               # 各节课语音（lecture/ex0…）
│   │   ├── words/  common/  grad/  py1/  py2/  s1~s4/   # 单词/通用/毕业/家长课
│   ├── vendor/
│   │   ├── skulpt/             # 纯 JS Python 引擎（代码练习真跑）
│   │   └── pyodide/            # 可选（打字挑战等，体积大，离线需本地 vendor）
│   └── tools/                  # 语音/视频生成脚本（作者用，见 §4）
├── license-author/             # ⚠️ 激活私钥与部署脚本（在网课工具「外」，绝不随包分发）
│   ├── private_key.pem         # RSA 私钥（发激活码用）
│   ├── public_key.pem
│   ├── author_gen.js           # 用私钥给机器码签名生成激活码
│   ├── admin.js                # list / revoke / gen
│   ├── worker/                 # （旧）Cloudflare Worker 在线方案代码，离线方案已弃用
│   ├── mock_server.js
│   └── DEPLOY.md               # 部署说明
└── .workbuddy/                 # 项目记忆（不随包）
```

---

## 2. 激活机制（离线一机一码 · 零服务器）

> **本副本（网课工具_无激活版）`SKIP_ACTIVATION=true`，已关闭激活、打开即用**；下方为激活系统完整设计（切 `false` 即启用离线一机一码）。

采用**离线一机一码**方案：激活码由作者私钥（RSA-2048）对「机器码」签名，前端只内嵌公钥验签——**无私钥造不出合法激活码，且不需要任何服务器 / 网络 / 域名**。

- **总开关**：`js/app.js` 顶部 `const SKIP_ACTIVATION`
  - `true` → 跳过激活页，直接进首页（本副本即此状态，免激活预览版）。
  - `false` → 启用离线「一机一码」：未激活只显示激活页；买家粘贴作者给的激活码，前端公钥验签通过即解锁。
- **机器码 / 激活码**
  - 机器码 = 持久随机串（存 `wb_mid_v1`，清缓存 / 换浏览器会变，属防白嫖正常代价），24 位。
  - 激活码 = base64(机器码 | 过期时间戳 | RSA-SHA256 签名 hex)，由 `author_gen.js` 用私钥签发；默认 **2 年**有效（可 `-y <年数>` 自定义）。
  - 前端用 `libs/jsrsasign.min.js` 验签（只内嵌公钥 `PUBLIC_KEY_PEM`，私钥 `license-author/private_key.pem` 不随包）。
- **换锁**：换 RSA 密钥对后，所有旧激活码全部作废，已激活设备需重新向作者获取激活码。

> 离线方案的代价：作者**无法远程注销单台设备**（无服务器就无"拉黑"能力）。如需远程吊销，须回到 Cloudflare Worker 在线方案（见 `license-author/` 历史文档）。

---

## 3. 更新与备份

- **一键更新器 `update.bat`**：在同级目录找含 `index.html` 的旧版文件夹，`xcopy` 覆盖；自动排除 `license-author/`、`.workbuddy/`、`node_modules/`、`.git/`。
- **进度存浏览器 `localStorage`**：课程进度、家长 PIN、昵称、语音风格、勋章、代码练习进度等。覆盖到**同一文件夹**（同名同路径）资料不丢。
- **导出 / 导入**：家长园地 → 💾 数据备份，生成 `我的课程进度.json`（含上述资料，**不含激活记录**——绑机器，换设备需重激活）。
- **备份"无激活完整版"**：直接 `cp -r 网课工具 网课工具_无激活版`，即得到一份打开即用、含全部内容的副本。

---

## 4. 视频 / 语音生成流水线（`tools/`）

> 依赖：Python3 + `edge-tts` + `Pillow`，以及本地 `ffmpeg`。合成时需联网（调用 TTS），产物离线播放。

**课程讲解文案**
- `narration.py`：`NARRATION`（逐点逐行详细讲解）与 `TALK`（一句话总结 + 生活发散 + 小提醒）**两份分离**；视频用 NARRATION，"讲一讲"按钮用 TALK。

**视频生成**
- `gen_matched.py`：PIL 逐帧 + ffmpeg 合成 mp4（已踩坑：每段只渲染 1 张关键帧 + ffmpeg `-loop 1`；`main` 末尾 `os._exit(0)` 强退避免 edge-tts 遗留连接挂起；脚本内勿调 `os.remove` 触发 safe-delete 拦截；中间文件留 `tools/_matched/` 不删）。
- `gen_ffmpeg_video.py` / `gen_ffmpeg_video_cute.py`：不同风格的 ffmpeg 封装。
- `gen_code_videos.py` / `gen_sample_videos.py` / `gen_all_videos.py`：批量生成代码课 / 样例 / 全部。

**语音生成**
- `extract_sources.js`：从 `course.js` 提取讲义/题目 → 按语义切句。
- `gen_audio.py`：edge-tts（晓晓）逐句合成 mp3 → 生成 `data/audio.js`（内联 `AUDIO_MAP`）。
- `gen_narrate.py` / `gen_talk.py`：合成"讲一讲"音频（TALK 文案）。
- `gen_vocab_audio.py`：单词发音（`audio/words`、`audio/common`）。
- `gen_voices.py` / `gen_check.py` / `gen_lesson_words.py`：多语音风格、校验、章节单词。
- `regen_learn_audiojs.py`：把"全部正确"评价语音烘焙进 `window.AUDIO_LEARN`（内联于 JS，运行时不读 `audio/_learn/` 目录——该目录为中间产物，可清理）。
- `regen_pron.py` / `pron.py`：发音相关。
- `extract_tts.js` / `add_video_fields.js` / `bake_py1_narrate.py` / `lessons_code.json`：辅助提取与字段补写。

**校验 / 测试**
- `validate_course.js`：校验 `course.js` 结构完整性。
- `test_*.js` / `test_runner.html`：课程/单词/滚动等自测。

**重新生成某节课**：改 `narration.py` 对应段 → 跑 `gen_matched.py` / `gen_audio.py` 等 → 刷新浏览器（bump `?v=`）。

---

## 5. 换课与改内容

- **`data/course.js`**（⭐ 核心）：课程树、讲义、练习、单词全在此；47 节结构（学前 2 家长课 + 14 章 + 4 进度总结 + 毕业）。
- **`data/code_practice.js`**：初级/中级各 20 题，填空模式（`____` 留空 + `answer` 完整代码 + `expect` 判分）。
- **`data/vocab-words.js` / `js/vocab.js`**：单词库。
- **封面图标**：`js/cover_icons.js` 的 `COVER_ICON`，按课程 emoji（去 FE0F 变体符）映射专属 SVG；改/增图标改这里。
- **模块图标**：`js/app.js` 顶部 `ICON` 对象（19 个：首页/园地/各功能），改这里。
- **练习判分**：`app.js` 内 choice / fill / open / order 四类判分逻辑。

---

## 6. 依赖与运行

- 纯前端 `file://`，双击 `index.html`（Chrome / Edge）。
- `vendor/skulpt`：浏览器内 Python（代码练习真跑；**不支持**真实文件 IO / 网络 / matplotlib / turtle，相关课用模拟）。
- `vendor/pyodide`：可选，离线需本地 vendor。
- `libs/jsrsasign.min.js`：RSA 验签（激活）。
- **防缓存**：`index.html` 里各 `<script>/<link>` 带 `?v=` 常量，改动后必须 bump，否则浏览器用旧缓存。

---

## 7. 发版清单

1. **清理中间文件**：删 `_` 前缀临时脚本/日志、样本视频、验证对比音频（`audio/sentences`、示例朗读类）、未引用预览页（`anim-demo.html` 等）、空目录。
2. **激活状态**：**本副本（网课工具_无激活版）`SKIP_ACTIVATION=true`，无激活预览版、打开即用**；若要发"有激活版"，改 `false` 即启用离线「一机一码」（无需部署任何后端）。
3. **打包范围**：只发 `网课工具/` 目录（**不含** `license-author/`、`电脑使用技巧/.workbuddy/`）。
4. **附说明**：使用者收《工具说明_用户版》；作者自留《工具说明_作者版》与 `license-author/`。
5. **一键更新**：随包带 `update.bat` + `更新说明.txt`。

---

## 8. 常见排错

- 打开 `index.html` 显示源码 → 用浏览器打开（非文本编辑器）。
- 某节课视频空白 → `video/` 缺该课 mp4，回去跑视频生成流水线补。
- 语音不播 → 检查音量 / 浏览器声音权限；或重新 `gen_audio.py`。
- 激活相关 → 查 `SKIP_ACTIVATION` 开关（本副本为 true，即未启用激活）；若启用激活版，确认买家激活码对应本机机器码、作者私钥与前端公钥配套。
